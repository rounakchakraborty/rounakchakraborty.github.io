name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  # Optional: allow manual deploys from the Actions tab
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Cancel any in-flight run of THIS workflow on THIS branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Recommended by GitHub for Pages builds
      - name: Setup Pages
        uses: actions/configure-pages@v5

      # Upload everything in the repo root (HTML/CSS/JS, images, docs, etc.)
      - name: Upload static site as artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .  # your site files live at the repo root

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
